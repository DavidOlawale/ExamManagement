@model Course

@{
    ViewBag.Title = Model.Name;
}

<div class="container-fluid">
    <div class="card card-custom min-h-450px">
        <div class="card-body">

            <ul class="nav nav-tabs" id="myTabContent" role="tablist">
                <li class="nav-item">
                    <a class="nav-link active" href="#">Details</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#subjects">Subjects</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#exams" tabindex="-1">Exams</a>
                </li>
            </ul>
            <div class="tab-content" id="myTabContent">
                <div class="tab-pane fade show active" id="pills-home" role="tabpanel" aria-labelledby="pills-home-tab">

                </div>
                <div class="tab-pane fade" id="subjects" role="tabpanel" aria-labelledby="pills-profile-tab">
                    <p>Subjects attached to <span class="font-weight-bold">@Model.Name</span></p>

                    <table>

                    </table>
                </div>
                <div class="tab-pane fade" id="exams" role="tabpanel" aria-labelledby="pills-contact-tab">

                </div>
            </div>
        </div>
    </div>

</div>
